//
//  PopUpViewController.swift
//  swyperio
//
//  Created by Jeremia Muhia on 12/8/16.
//  Copyright © 2016 NYU. All rights reserved.
//

import UIKit

class PopUpViewController: UIView {
    
    //override func viewDidLoad() {
        //super.viewDidLoad()

        // Do any additional setup after loading the view.
        // var datePicker: UIDatePicker = UIDatePicker()
        // self.datePicker.datePickerMode = UIDatePickerMode.dateAndTime
        // datePicker.frame = CGRect(x: UIScreen.main.bounds.midX, y: UIScreen.main.bounds.midY, width: 100, height: 100)
        // datePicker.frame = CGRect(x: UIScreen.main.bounds.midX, y: <#T##CGFloat#>, width: <#T##CGFloat#>, height: <#T##CGFloat#>)
    //}

    // override func didReceiveMemoryWarning() {
        // super.didReceiveMemoryWarning()
        // Dispose of any resources that can be recreated.
    // }
    

    /*
    // MARK: - Navigation

    // In a storyboard-based application, you will often want to do a little preparation before navigation
    override func prepare(for segue: UIStoryboardSegue, sender: Any?) {
        // Get the new view controller using segue.destinationViewController.
        // Pass the selected object to the new view controller.
    }
    */

}
